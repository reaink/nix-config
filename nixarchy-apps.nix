# App selection for programs.nixarchy.apps, managed by the Omarchy Install
# menu: picks write to ~/.config/nixarchy/apps.nix and `nixarchy-apply` copies
# that file here (flake root) before rebuilding. Edit via the menu, not by hand.
{ programs.nixarchy.apps = { }; }
